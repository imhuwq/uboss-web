<%= simple_form_for current_user, url: account_path, method: :PATCH do |f| %>
  <span>账户名称:<span> <%= f.input :nickname %>
  <%= f.submit '修改' %>
<% end %>

<div>
  <span>收货地址<span>
  <%= link_to '添加/修改', account_user_addresses_path %>
</div>
