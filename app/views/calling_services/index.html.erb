<%= content_for :title do %>
  呼叫服务
<% end %>

<p>
  <a href="<%= table_numbers_seller_calling_services_path(seller_id: params[:seller_id]) %>"><%= cookies[:table_nu] %>号桌</a>
  <a href="<%= notifies_seller_calling_services_path(seller_id: params[:seller_id]) %>">呼叫详情</a>
</p>

<div style="width:50%;margin:50% auto;">
  <br />
  <% @calling_services.each do |calling_service| %>
    <p>
    <span><%= calling_service.name %></span>
    <a href="#">呼叫</a>
    </p>
  <% end %>

  <p>
  <span>结帐</span>
  <a href="#">呼叫</a>
  </p>
</div>

<a href="#" class="btn btn-large btn-block btn-orange" style="position:fixed;bottom:0;">其他服务，一键呼叫</a>
